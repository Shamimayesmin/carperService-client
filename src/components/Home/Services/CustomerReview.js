import React from 'react';

const CustomerReview = (review) => {
    console.log(review);
    return (
        <div>
            thsi is review
        </div>
    );
};

export default CustomerReview;